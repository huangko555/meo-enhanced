# MEO Enhanced 0.3.7 撤销/重做与输入稳定性长测记录

日期：2026-08-30

测试文档：`D:\Desktop\hkq_projects\meo_test_md_local\meo-undo-redo-uat.md`

测试前后 SHA-256：`472EB74FF357E89436F203562DC940AB2968497439B874B872C8339954538C2C`

## 测试目标

使用真实 410 行 UAT 文档，对普通文本、行内格式、标题、列表、任务、引用、短/长/嵌套代码块、链接、图片、脚注、行内/块级公式、Mermaid、多个 Markdown 表格、HTML details/div/table 和文档首尾执行连续及固定种子乱序编辑。

每次操作同时检查：

- 完整文档内容是否精确匹配预期；
- 普通与快速撤销/重做是否精确回到对应版本；
- 焦点、光标和目标元素是否位于正确位置；
- 目标已在屏幕内时不滚动，目标在屏幕外时定位到目标；
- 输入期间是否出现无关滚动、目标跳动、源码闪烁或焦点丢失；
- 上方公式、Mermaid 等块增删行后，下方表格位置映射是否仍然正确。

自动化在隔离的生产编辑器运行时中读取文档，不保存回测试文件。

## 最终耐久测试结果

- 编辑目标：56 项；
- 普通撤销：56 次；
- 普通重做：56 次；
- 快速撤销：56 次；
- 快速重做：56 次；
- 核对历史命令：224 个；
- 视觉采样：1052 帧；
- 最大编辑滚动跨度：1px；
- 最大目标位置跨度：0px；
- 软异常：0；
- 测试文档前后哈希一致。

结果：严格模式全绿。

## 本轮确认修复

- 上方嵌入块增行后，表格不再使用过期源位置提交；
- 表格输入、撤销和重做在目标可见时不再产生非预期窗口跳动；
- 屏幕外历史目标会先物化并定位，屏幕内目标保持当前视口；
- Mermaid 和块级公式在 Preview/Source/分栏间切换后可正常退出，历史恢复不会破坏块模式；
- 窄窗口首次打开表格时自动进入弹性总宽，按启动默认宽度为上限恢复；
- 手动列宽比例在弹性总宽变化中保留；
- 可读最小列宽能够放下时不产生横向滚动，也不裁掉右边框；仅最小宽度本身放不下时允许横向滚动；
- 主表与 Sticky 表头共享列宽和总宽，拖拽线及边框保持对齐；
- 表格位置映射、diff 刷新、输入视觉稳定和可见历史视口专项回归通过。

## 交叉与正式门禁

- 严格 TypeScript：扩展端和 Webview 均通过；
- 架构检查：215 个源文件、572 条静态导入，`Legacy=[]`；
- 共享模块架构契约：通过；
- 正式 `bun run test` 总门禁：通过；
- 单元门禁：71 项通过；
- 浏览器门禁：60 项通过；
- 表格列宽生产级 Chromium 测试：连续三轮通过；
- 历史矩阵：连续三轮通过，每轮 20 次撤销、20 次重做及 held-Control 混合压力；
- 渲染块历史压力：12 次撤销、12 次重做及快速/长按组合通过；
- 主题跟随、目录同步、嵌入输入视口、表格 diff/位置/视觉稳定等新增专项测试全部通过。

## 结论

本轮发现的撤销/重做、焦点、窗口定位、表格源位置、弹性宽度及 Mermaid/公式模式问题已完成修复和自动化回归。当前自动化验收结果为全绿，可进入 F5 最终人工冒烟测试。
